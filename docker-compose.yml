version: '3.8'

services:
  apk-generator:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./uploads:/app/uploads
      - ./builds:/app/builds
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
